class Heap:
    def __init__(self, array=None, size=None):
        if harray != None:
            self.harray = self.heapify(array, size)
        else:
            self.harray = list()

    def heapify(self, array, size):
        harray = [None for i in range(size)]

    def heap_sort(self,):

    def __str__(self):
        return f"{" ".join(self.harray)}"
